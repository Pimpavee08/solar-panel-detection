# ระบบบัญชีผู้ใช้

ตาราง `User` ใน ER ถูกนำมาใช้งานจริงแล้ว และทุก Task ผูกกับเจ้าของผ่าน `Task.uid`

## สรุปการทำงาน

```
เบราว์เซอร์ ──POST /auth/login──► FastAPI ──► ตรวจ bcrypt กับ User.passwd
                                                │
                                                ▼
                               ตั้ง session cookie (เซ็นด้วย SOLAR_SECRET_KEY)
                                                │
เบราว์เซอร์ ──GET /tasks + cookie──► FastAPI ──► WHERE task.uid = <ผู้ใช้ปัจจุบัน>
```

Airflow ไม่เกี่ยวกับการยืนยันตัวตนเลย — มันเขียน `Task_Step` / `Task_Result` ลง
ฐานข้อมูลโดยตรง ส่วน `uid` ถูกกำหนดตั้งแต่ตอนที่หน้าเว็บสร้าง Task

## Endpoint

| Endpoint | ต้องล็อกอิน | หมายเหตุ |
|---|---|---|
| `GET /` | ไม่ | หน้าเว็บ (มีหน้าล็อกอินในตัว) |
| `GET /health` | ไม่ | ใช้ตรวจว่าระบบขึ้นหรือยัง |
| `POST /auth/register` | ไม่ | สมัครแล้วล็อกอินให้เลย |
| `POST /auth/login` | ไม่ | |
| `POST /auth/logout` | ไม่ | |
| `GET /auth/me` | ใช่ | |
| `/tasks` ทั้งหมด | ใช่ | เห็นและแก้ได้เฉพาะงานของตัวเอง |

## การเก็บรหัสผ่าน

สเปกระบุคอลัมน์ `passwd` เป็น string — คงชื่อคอลัมน์ไว้ แต่เก็บเฉพาะ hash

วิธี hash คือ **sha256 → base64 → bcrypt** ที่ต้องย่อด้วย sha256 ก่อนเพราะ bcrypt
อ่านได้แค่ 72 ไบต์แรกแล้วตัดส่วนเกินทิ้งเงียบ ๆ ภาษาไทยหนึ่งตัวกิน 3 ไบต์ใน UTF-8
รหัสผ่านไทยราว 24 ตัวก็ชนเพดานแล้ว ส่วนที่แปลงเป็น base64 ก่อนก็เพราะ bcrypt
ใช้ไบต์ 0 เป็นจุดจบสตริง ซึ่ง digest ดิบอาจมีปนอยู่

## การป้องกันที่ใส่ไว้

- **งานของคนอื่นตอบ 404 ไม่ใช่ 403** — ถ้าตอบ 403 จะรู้ทันทีว่า `tid` นั้นมีอยู่จริง
- **ล็อกอินผิดไม่บอกว่าผิดตรงไหน** — ตอบ "อีเมลหรือรหัสผ่านไม่ถูกต้อง" เสมอ และยัง
  คำนวณ hash ทิ้งแม้ไม่พบอีเมล เพื่อไม่ให้เดาได้จากเวลาตอบกลับว่าอีเมลนี้มีอยู่ไหม
- **cookie เป็น HttpOnly + SameSite=Lax** JavaScript อ่านไม่ได้
- **CORS ไม่ใช้ `*` อีกต่อไป** — เบราว์เซอร์ปฏิเสธการส่ง cookie ข้ามโดเมนเมื่อ origin
  เป็น wildcard ตั้งรายการโดเมนผ่าน `SOLAR_CORS_ORIGINS`

## ตัวแปรสภาพแวดล้อม

| ตัวแปร | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `SOLAR_SECRET_KEY` | *(สุ่มใหม่ทุกครั้งที่สตาร์ต)* | คีย์เซ็น session cookie |
| `SOLAR_HTTPS` | *(ว่าง)* | ตั้งเป็น `1` เมื่อเสิร์ฟผ่าน HTTPS เพื่อบังคับ Secure flag |
| `SOLAR_CORS_ORIGINS` | `http://localhost:8008,http://localhost:8009,http://127.0.0.1:8008` | โดเมนที่เรียก API ข้ามโดเมนได้ |

**ต้องตั้ง `SOLAR_SECRET_KEY` บนเซิร์ฟเวอร์จริง** ไม่งั้นผู้ใช้ทุกคนจะถูก logout
ทุกครั้งที่ deploy เพราะคีย์เปลี่ยนไป

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

## จัดการผู้ใช้จากบรรทัดคำสั่ง

```bash
python manage_users.py create --name "ชื่อ" --email a@b.c   # ถามรหัสผ่านตอนรัน
python manage_users.py list
python manage_users.py reset-password --email a@b.c
python manage_users.py adopt-orphans --email a@b.c
```

รหัสผ่านรับทาง prompt เท่านั้น ไม่รับเป็น argument เพราะจะติดอยู่ใน shell history
และมองเห็นได้จากรายการโปรเซสของเครื่อง

`adopt-orphans` มีไว้สำหรับ Task ที่สร้างไว้ก่อนจะมีระบบล็อกอิน ซึ่ง `uid` เป็น NULL
จึงไม่มีใครมองเห็นได้เลย คำสั่งนี้โอนงานเหล่านั้นให้ผู้ใช้ที่ระบุ

ถ้าใช้ Postgres ต้องตั้ง `SOLAR_DATABASE_URL` ก่อนรันคำสั่งพวกนี้ ไม่งั้นมันจะไปแก้
ไฟล์ SQLite แทน

## ที่ยังไม่ได้ทำ

- **ยังไม่มีการจำกัดจำนวนครั้งที่ลองล็อกอิน** ถ้าเปิดให้เข้าถึงจากภายนอกควรใส่
  rate limit ที่ `POST /auth/login` ก่อน
- **ยังไม่มีการรีเซ็ตรหัสผ่านด้วยตัวเอง** ต้องให้ผู้ดูแลรันคำสั่ง `reset-password`
- **ยังไม่มีบทบาท (role)** ทุกคนเห็นเฉพาะงานตัวเองเท่ากันหมด ไม่มีผู้ดูแลที่เห็นทุกงาน
