# สรุปการพัฒนา

เอกสารนี้เล่าว่าโปรเจกต์เริ่มจากอะไร พัฒนาต่อเป็นอะไร และแต่ละอย่างทำไปเพราะอะไร
สำหรับดูภาพรวมของระบบให้ดู [README.md](README.md)

---

## โค้ดตั้งต้น

ระบบเดิมทำงานได้ครบวงจรแล้ว มี 9 ไฟล์ Python รวมราว 1,400 บรรทัด

| ไฟล์ | ทำอะไร |
|---|---|
| `app.py` (549 บรรทัด) | REST API + หน้าเว็บ HTML ฝังอยู่ในไฟล์เดียวกัน |
| `data_retrieval.py` | ต่อ tile จาก Google เป็น GeoTIFF |
| `model_runner.py` | สร้าง `.toml` แล้วเรียกโมเดล |
| `post_processing.py` | shapefile → GeoJSON + คำนวณพื้นที่/จำนวนแผง/kWp |
| `solar_pipeline_dag.py` | Airflow DAG (เขียนไว้แต่ยังไม่มีอะไรเรียกใช้) |
| `main.py` | รันทั้ง pipeline จากบรรทัดคำสั่ง |
| `mock_model.py` | โมเดลจำลองสำหรับทดสอบ |
| เทสต์ 2 ไฟล์ | รวม 7 ข้อ |

**สิ่งที่ทำได้แล้ว:** กรอกพิกัด → ดึงภาพ → รันโมเดล → ได้ GeoJSON + ตัวเลขสรุป → แสดงบนแผนที่

**ช่องว่างหลัก:**

- ฐานข้อมูลเป็นตารางเดียว `job_results` ไม่ตรงกับ ER ในเอกสาร AW3 ที่ระบุ 4 ตาราง
- ไม่มีระบบผู้ใช้ ใครเปิดหน้าเว็บก็เห็นและลบงานของคนอื่นได้ทั้งหมด
- pipeline รันใน `BackgroundTasks` ของ FastAPI — รีสตาร์ตเว็บแล้วงานหาย พังแล้วไม่ลองใหม่
- DAG มีอยู่แต่ไม่ได้ต่อกับอะไร และใช้โครงสร้างพาธคนละแบบกับตัวที่รันจริง
- หน้าเว็บให้กรอกละติจูด/ลองจิจูดเป็นตัวเลข 4 ช่อง
- ไม่รู้ว่างานติดอยู่ขั้นไหน สถานะมีแค่ `running` / `completed` / `failed`

---

## สิ่งที่พัฒนาเพิ่ม

รวม **39 ไฟล์ · +4,831 / −961 บรรทัด** แบ่งเป็น 6 ระยะ

### 1. ออกแบบหน้าเว็บใหม่

ย้าย HTML ออกจาก `app.py` ไปเป็น [static/dashboard.html](static/dashboard.html) แล้วทำใหม่เป็น
แผนที่เต็มจอแบบเครื่องมือ GIS

- **ลากกรอบเลือกพื้นที่บนแผนที่** แทนการกรอกพิกัด 4 ช่อง พร้อมบอกขนาดเป็น ตร.กม. ขณะลาก
- **นับจำนวน tile สด ๆ** (x/100) แล้วปิดปุ่มเริ่มงานถ้าเกินขีดจำกัด — เดิมต้องกดไปก่อนแล้วค่อยรู้ว่าพัง
- เลือกระดับซูมได้ 17–20 พร้อมบอกความละเอียดเป็นเมตร/พิกเซล
- สลับแผนที่ฐาน เปิด/ปิดชั้นข้อมูล ปรับความทึบ เครื่องมือวัดระยะและพื้นที่
- รายการแผงรายแปลง คลิกแล้วซูมไปหา เรียงจากใหญ่ไปเล็ก
- ส่งออก CSV รายแปลง

### 2. เปลี่ยนฐานข้อมูลให้ตรง ER และทำ pipeline ใหม่

สร้าง 4 ตารางตาม ER ในเอกสาร AW3 ด้วย SQLAlchemy — `User` / `Task` / `Task_Step` / `Task_Result`
โดยใช้ uuid เป็น primary key และย้ายพาธไฟล์ไปอิงกับ `tid`

แยก pipeline เป็น 4 ขั้นตามเอกสาร พร้อมกฎ retry:

```
generating_config → fetch_image → run_inference → parse_result
```

| กฎในเอกสาร | ที่ทำจริง |
|---|---|
| หนึ่ง Task มี 4 step พอดี | สร้าง 4 แถว `pending` ตั้งแต่ตอนสร้าง Task |
| `Task.status = step + ":" + status` | เช่น `run_inference:running` |
| retry สูงสุด 5 ครั้ง | ทดสอบแล้ว: ล้มเหลว → ลองซ้ำ 5 ครั้งพอดี → `failed` |
| ล้างโฟลเดอร์ก่อน retry | ล้างเฉพาะของที่ step นั้นสร้างเอง (ดูหมายเหตุด้านล่าง) |
| เขียน `Task_Result` เมื่อ `parse_result` สำเร็จ | ถ้าล้มก่อนหน้าจะไม่มีแถวผลลัพธ์เลย |

ทำให้หน้าเว็บแสดง **ความคืบหน้าราย step** ได้จริง อ่านจากตาราง ไม่ใช่เดาจากไฟล์บนดิสก์

### 3. ภาพ overlay

`Task_Result.overlay_image_path` ใน ER ยังว่างอยู่ และ Requirement Spec ข้อ 7 ระบุว่าต้อง
วาดผลการตรวจจับทับภาพต้นฉบับ จึงเขียน [overlay.py](overlay.py) ให้ออกมา 3 ไฟล์

- `overlay.png` — ภาพที่วาดขอบเขตแผงทับแล้ว
- `overlay.pgw` — world file เปิดใน QGIS แล้วอยู่ตำแหน่งถูกต้อง
- `overlay.json` — ขอบเขตพิกัดให้หน้าเว็บวางเป็นชั้นภาพ

ขอบเขตอ่านจากไฟล์ raster จริง ไม่ใช่จากกรอบที่ผู้ใช้ลาก เพราะภาพที่ต่อจาก tile จะกว้างกว่า
กรอบเล็กน้อยจากการปัดขอบของ tile ถ้าใช้ค่าที่ผู้ใช้ลากภาพจะเหลื่อมกับแผนที่

### 4. ย้ายตัวรันไปใช้ Airflow

เอกสาร Requirement ยก *Workflow Management* เป็น Main Challenge และระบุ Apache Airflow ไว้
แต่ DAG เดิมไม่ได้ต่อกับอะไร จึงเขียนใหม่ให้ **หนึ่ง Airflow task = หนึ่ง step**

- การ retry ยกให้ Airflow จัดการ (`retries = 4` จึงได้ 5 ครั้งรวมครั้งแรก) แล้ว callback สะท้อน
  สถานะกลับลง `Task_Step`
- FastAPI แค่บันทึก Task แล้วสั่ง DAG run หนึ่งครั้ง จากนั้นไม่ถาม Airflow อีกเลย — สถานะทั้งหมด
  อ่านจากฐานข้อมูล ทำให้หน้าเว็บใช้งานต่อได้แม้ Airflow ล่ม
- มี `docker-compose.yml` ตั้ง Airflow + PostgreSQL ให้พร้อมใช้
- **ยังรันแบบไม่มี Airflow ได้** ถ้าไม่ตั้ง `SOLAR_AIRFLOW_URL` เว็บจะรันเองเหมือนเดิม สะดวก
  ตอนพัฒนาและตอนรันเทสต์

### 5. ระบบบัญชีผู้ใช้

ตาราง `User` ใน ER มีอยู่แต่ไม่เคยถูกใช้ และ `Task.uid` เป็น NULL เสมอ แปลว่าใครก็อ่าน สั่งรัน
และลบงานของคนอื่นได้หมด

- เก็บรหัสผ่านเป็น hash เท่านั้น ใช้ **sha256 → base64 → bcrypt** เพราะ bcrypt อ่านได้แค่ 72 ไบต์
  แล้วตัดส่วนเกินทิ้งเงียบ ๆ ภาษาไทยหนึ่งตัวกิน 3 ไบต์ รหัสผ่านไทยราว 24 ตัวก็ชนเพดานแล้ว
- session cookie แบบเซ็น HttpOnly + SameSite=Lax
- งานของคนอื่นตอบ **404 ไม่ใช่ 403** เพื่อไม่ให้เดาได้ว่า `tid` ไหนมีอยู่จริง
- ล็อกอินผิดตอบข้อความเดียวกันเสมอ และยังคำนวณ hash ทิ้งแม้ไม่พบอีเมล เพื่อไม่ให้เดาจากเวลา
  ตอบกลับว่าอีเมลนั้นมีในระบบหรือไม่
- จำกัดการลองล็อกอิน 10 ครั้งต่อ 5 นาทีต่อหนึ่ง IP
- [manage_users.py](manage_users.py) สำหรับสร้างผู้ใช้และโอนงานเก่าที่ยังไม่มีเจ้าของ

### 6. รวมหน้าจอของ Airflow เข้ามา

เดิมต้องเปิดสองเว็บ — ของเราดูแผนที่ ของ Airflow ดูว่าทำไมพัง ตอนนี้ย้ายมาอยู่ที่เดียว

- ปุ่ม `log` ข้างแต่ละ step ดึง log จริงจาก Airflow มาแสดง เลือกดูได้ทีละครั้งที่ retry
- ปุ่มสั่งรันใหม่ทั้งงาน (รีเซ็ต step ทั้ง 4 ลบผลลัพธ์เก่า แล้วสั่ง dag run ใหม่)
- แสดงเวลาที่ใช้ของแต่ละ step

---

## บั๊กที่เจอและแก้

### ภาพดาวเทียมมีรูดำได้โดยไม่มีใครรู้

`data_retrieval.py` เดิม เมื่อ tile ไหนโหลดไม่สำเร็จจะแค่ `print` แล้วไปต่อ

```python
else:
    print(f"Failed to fetch tile {tile.x}, {tile.y}, {tile.z}")   # แล้วไปต่อเฉย ๆ
```

พื้นที่ 256×256 ตรงนั้นเหลือเป็นสีดำ ถูกเซฟลง GeoTIFF ตามปกติ `fetch_image` ขึ้น `completed`
โมเดลตรวจพื้นที่ดำไม่เจอแผง **ตัวเลขจึงต่ำกว่าจริงโดยไม่มีอะไรบันทึกไว้เลย**

แก้เป็น retry ราย tile 3 ครั้ง ถ้ายังไม่ได้ให้โยน error ให้ step ล้มเหลวจริง เหตุผลจะไปโผล่ใน
`Task_Step.error_msg` และถูกกว่าเดิมมากเพราะเดิมพลาด tile เดียว Airflow ต้องโหลดใหม่ทั้ง 100 tile

### `subprocess.run(list, shell=True)` ไม่ทำงานบน Linux

บน Windows Python รวม list ให้เอง แต่บน POSIX จะรันแค่สมาชิกตัวแรก คือ `python` เปล่า ๆ
อ่าน stdin เจอ EOF แล้วคืน exit 0 — `run_inference` จึง **รายงานว่าสำเร็จทั้งที่ไม่ได้เรียกโมเดลเลย**
บั๊กนี้ซ่อนอยู่ในโค้ดเดิมมาตลอดเพราะไม่เคยรันบน Linux จนกระทั่งย้ายเข้า container

### retry แล้วลบผลงานของ step ก่อนหน้า

เอกสารเขียนว่า *"ล้างโฟลเดอร์ก่อน retry ยกเว้น input"* ซึ่งถูกเมื่อ retry ทั้ง pipeline
แต่ Airflow retry ทีละ step การล้างแบบเหมารวมทำให้ `parse_result` ลบ `output/` ที่ `run_inference`
สร้างไว้ retry อีก 4 ครั้งก็เจอ `No shapefile found` เหมือนเดิม แก้เป็นล้างเฉพาะของที่ step นั้นผลิตเอง

### image ของ Airflow ทำ ORM ของตัวเองพัง

ติดตั้ง `requirements.txt` ทั้งไฟล์ลง image ทำให้ pip อัปเกรด SQLAlchemy เป็น 2.x ทับ 1.4 ที่
Airflow 2.10 ผูกไว้ ผลคือ auth manager โหลดไม่ขึ้นและ `airflow users create` หายไปทั้งคำสั่ง
แก้เป็นแยก `requirements-airflow.txt` เฉพาะไลบรารีที่ DAG ต้องใช้ ติดตั้งคู่กับ constraints ของ Airflow

### CORS ตั้งค่าที่ใช้ไม่ได้จริง

เดิมตั้ง `allow_origins=["*"]` คู่กับ `allow_credentials=True` ซึ่งเบราว์เซอร์ปฏิเสธอยู่แล้ว และพอ
เปลี่ยนมาใช้ cookie มันจะพังจริง เปลี่ยนเป็นรายการโดเมนที่ระบุ

---

## การทดสอบ

| | เดิม | ปัจจุบัน |
|---|---|---|
| จำนวนเทสต์ | 7 | **70** |
| ใช้ฐานข้อมูลจริงตอนเทสต์ | ใช่ | ไม่ (ใช้ฐานข้อมูลชั่วคราว) |
| เทสต์ตัวที่คำนวณตัวเลข | ไม่มี | 14 ข้อ |

เทสต์เดิมข้อหนึ่งพังอยู่แล้วตั้งแต่ commit แรก (คาดหวัง JSON แต่ `/` คืน HTML)

[test_calculations.py](test_calculations.py) ตรึงสูตรไว้ด้วยรูปทรงที่คำนวณมือได้ —
สี่เหลี่ยม 10×10 เมตร ต้องได้ **100 ตร.ม. · 39 แผง · 18 kWp · 20,695.5 kWh/ปี** ซึ่งล็อกค่าคงที่
ทั้งสี่ตัวไว้พร้อมกัน (พื้นที่แผง 2.541 · 180 Wp/ตร.ม. · แดด 4.2 ชม./วัน · performance ratio 0.75)

**ตรวจสอบว่าเทสต์ใช้ได้จริง** ด้วยการแก้โค้ดให้ผิดแล้วดูว่าจับได้ไหม

| แก้อะไร | ผล |
|---|---|
| `wp_per_m2` 180 → 200 | 2 ข้อล้มเหลว |
| performance ratio 0.75 → 0.8 | 1 ข้อล้มเหลว |
| เอา retry ของ tile ออก | 2 ข้อล้มเหลว |

---

## จุดที่ทำต่างจากเอกสาร

ทุกข้อมีคอมเมนต์อธิบายไว้ในโค้ดแล้ว

| เรื่อง | เอกสารเขียนว่า | ที่ทำจริง | เพราะ |
|---|---|---|---|
| `Task.zoom` | ไม่มีในตาราง | เพิ่มคอลัมน์ | หน้าเว็บให้เลือกระดับซูมได้ ต้องเก็บไว้รันซ้ำ |
| `Task.dag_run_id` | ไม่มีในตาราง | เพิ่มคอลัมน์ | ถ้าไม่เก็บ ต้องไล่หา log จาก dag run ทั้งหมดใน Airflow |
| ล้างโฟลเดอร์ก่อน retry | ล้างทั้งหมดยกเว้น input | ล้างเฉพาะของที่ step นั้นสร้าง | ไม่งั้น retry ราย step จะลบผลงานของ step ก่อนหน้า |
| `User.passwd` | ชนิด string | คงชื่อคอลัมน์ เก็บ bcrypt hash | ห้ามเก็บรหัสผ่านดิบ |
| ภาพ overlay ล้มเหลว | ไม่ได้ระบุ | ไม่ทำให้ทั้ง Task ล้มเหลว | ตัวเลขผลลัพธ์คำนวณถูกไปแล้ว ไม่ควร retry ทั้งงานเพราะวาดภาพไม่ผ่าน |

---

## ที่ยังไม่ได้ทำ

- **ยังไม่ได้ต่อโมเดลจริง** — ต้องมี `run.sh` และ MLflow registry จากอาจารย์ก่อน
  ตอนนี้ `run_inference` ถอยไปใช้ `mock_model.py` อัตโนมัติ ตัวเลขทั้งหมดจึงเป็นตัวเลขทดสอบ
- **การคำนวณยังใช้ค่าคงที่** สมมติหลังคาแบนทั้งหมด ไม่ได้คิดมุมเอียง ทิศหัน หรือเงาบัง
  ถ้าจะทำต่อต้องมีข้อมูลความสูงพื้นผิว (DSM) ซึ่งภาพดาวเทียมให้ไม่ได้
- **ข้อมูลรายแปลงอยู่ในไฟล์ GeoJSON ไม่ได้อยู่ในฐานข้อมูล** พื้นที่ใหญ่ที่มีหลายพันแปลงจะเริ่มช้า
- **ยังไม่มี role** ทุกคนเห็นเฉพาะงานตัวเอง ไม่มีผู้ดูแลที่เห็นได้ทั้งหมด
- **ดาวน์โหลด tile ทีละแผ่น** 100 tile ใช้เวลาราว 20 วินาที ทำขนานได้
- **ค่าใน `docker-compose.yml` เป็นค่าสำหรับพัฒนาเท่านั้น** ต้องเปลี่ยนและตั้ง `SOLAR_SECRET_KEY`
  ก่อนนำขึ้นเซิร์ฟเวอร์จริง
